<nav>
  <div class="container">
    <div class="nav-wrapper">
      <a href="#" class="left brand-logo"><i class="material-icons">cloud</i>Howard's TODO</a>
      <ul class="right">
        <% if current_user %>
          <li>
            Salutations, <%= current_user.email %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete do %>
              <i class="material-icons">exit_to_app</i>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
