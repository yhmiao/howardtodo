<%= render "todos/partials/notice", notice: notice %>

<h4 class="center"><%= @todo.title %></h4>

<div class="card">
  <div class="card-content">
    <p><%= @todo.body %></p>
  </div>
</div>

<div class="center">
  <%= link_to 'Edit', edit_todo_path(@todo) %> | <%= link_to 'Back', todos_path %>
</div>
